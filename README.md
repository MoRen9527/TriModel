# TriModel

TriModel 是三元宇宙中的 Provider/Model 统一配置层。

- 模块定位：负责多 provider 适配、模型路由、fallback 链配置，为 `TriMC` 与 `Tride` 两个 orchestration 提供统一的模型接入点。
- 当前状态：当前待初始化 / 待接入。
- 当前要求：本仓按公司统一基线维护独立 git、README、docs 六件套与本地 CodeGraph。
